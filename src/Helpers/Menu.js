const handleMenuLinks = (e) =>{
    // const clickedElement = e.target;
    // const activeLink = document.getElementsByClassName("active")[0];
    // activeLink.classList.remove("active");
    // clickedElement.classList.add("active");
    // clickedElement.classList.add("item");
    // clickedElement.getAttribute("className");
    // clickedElement.setAttribute("class", ("active item"))
    // console.log(activeLink.classList)
    // console.log(clickedElement.classList)
}

export default handleMenuLinks;